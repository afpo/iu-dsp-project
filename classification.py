import pandas as pd
import numpy as np
from math import sqrt
import sqlite3

# Load datasets

# Matches from Step 3

# Compute max deviations for each matched pair

# Allowed deviation = sqrt(2) * max deviation

# Prepare interpolation grid (global)

# Classification
## Interpolate ideal function value at x_test
## Check allowed deviation

# Convert to DataFrame

# Save CSV

# Create matches summary CSV

# Save both tables to SQLite
